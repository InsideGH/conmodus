import React from 'react';
import { VERSION } from '../../config';

const Login = () => {
    return (
        <>
            <h5>Login page. Version {VERSION}</h5>
        </>
    );
};

Login.propTypes = {};

export default Login;
