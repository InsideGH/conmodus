version: '3.7'
services:

  frontend_dev:
    build:
      context: ./services/frontend
      dockerfile: Dockerfile.dev
    ports:
      - "80:80"
    networks:
      - net_a
    environment:
      NODE_ENV: development
      LOG_LEVEL: debug
    volumes:
      - ./services/frontend/src:/app/src

networks:
  net_a: